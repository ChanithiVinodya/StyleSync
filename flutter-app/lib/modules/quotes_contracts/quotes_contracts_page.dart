import 'package:flutter/material.dart';

// OWNED BY: Student 3 - Quotes & Contracts
class QuotesContractsPage extends StatelessWidget {
  const QuotesContractsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('TODO: view AI proposal, approve / reject / request changes'),
    );
  }
}
