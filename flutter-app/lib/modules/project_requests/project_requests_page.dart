import 'package:flutter/material.dart';

// OWNED BY: Student 2 - Project Requests & Room Uploads
// This is the primary client-facing screen: room details, camera upload,
// budget, preferences -> submits and triggers the Agentic AI workflow.
class ProjectRequestsPage extends StatelessWidget {
  const ProjectRequestsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('TODO: room makeover request form + camera upload'),
    );
  }
}
