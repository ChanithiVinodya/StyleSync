import 'package:flutter/material.dart';

// OWNED BY: Student 4 - Project Execution & Progress Tracking
class ProjectExecutionPage extends StatelessWidget {
  const ProjectExecutionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('TODO: milestones + photo timeline'),
    );
  }
}
