# Project Execution & Progress Tracking — Student 4

Build here:
- `Models/` — your entities (e.g. `ProjectMilestone`, `ProjectTask`, `ProgressPhoto`)
- `Data/` — `IEntityTypeConfiguration<T>` classes for EF Core
- `Controllers/` — your API endpoints (milestones, tasks, photo timeline)
- `Services/` — delay-detection logic, progress calculation

**Reminder:** add your `DbSet<T>` line to `AppDbContext.cs` under the
"Project Execution" section only.
